apiVersion: v1
kind: Service
metadata:
  name: {{ include "journarry-web.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "journarry-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "journarry-web.selectorLabels" . | nindent 4 }}
